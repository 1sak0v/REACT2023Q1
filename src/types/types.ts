export interface ISearchState {
  search: string;
}

export interface ISearchProps {
  test?: string;
}

export type TData = {
  description: string;
  id: number;
  name: string;
  thumbnail: string;
};
